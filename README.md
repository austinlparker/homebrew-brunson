# austinlparker/brunson

A [Homebrew tap](https://docs.brew.sh/Taps) for the [`brunson`](https://github.com/austinlparker/brunson) terminal PR manager.

## Usage

```bash
brew tap austinlparker/brunson
brew install brunson
```

After installing, run `brunson --version` to verify the binary is available.
